импорт java.util.Random;
импорт java.util.Scanner;
публичный класс konsole {
    public static void main(String[] args) {
         Сканирование сканера = новый сканер(System.in);
        int min = 30, max = 70;
        Random random = новый Random();
        System.out.print("элементы подсчета входных данных:");

//создать
        int masCount = scan.nextInt();
        int[] mas = новый int[masCount];
// for (int i = 0; i < mas.length; i++) {
// mas[i] = random.nextInt(max - min + 1) + min;
//        }
        for (int i = 0; i < mas.length; i++) {
            System.out.println("intput current elements #:"+i);
            mas[i] = scan.nextInt();
        }

        //извлечь
        System.out.println("output mas:");
        for (int i = 0; i < mas.length; i++) {
            System.out.print(mas[i] + " ");
        }

        System.out.println();

        // обновить
        System.out.println("Апдейт");
        for (int i = 0; i < mas.length; i++) {
            if(mas[i]<40)
            mas[i] = mas[i] + 5;
        }

        System.out.println("output mas:");
        for (int i = 0; i < mas.length; i++) {
            System.out.print(mas[i] + " ");
        }
        System.out.println();

        //удалить
        mas[1] = -1;
        System.out.println("output mas:");
        for (int i = 0; i < mas.length; i++) {
            if (mas[i] != -1) {
                System.out.print(mas[i] + " ");
            }
        }
        System.out.println();
        for (int i = 0; i < mas.length; i++) {
            if (mas[i] % 2 == 0) {
                System.out.print(mas[i] + " ");
            }
        }

        System.out.println();

    }

}